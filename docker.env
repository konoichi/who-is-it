# CODE-ID: docker_env_v1
#
# Zentrale Konfigurationsdatei für das Deployment-Skript.
# Trage hier deine spezifischen Daten ein.

# --- Gitea & Image Konfiguration ---

# Dein Gitea-Benutzername
GITEA_USER=stephan

# Die URL deiner Gitea-Instanz (ohne https://)
GITEA_REGISTRY_URL=git.wlkns.org

# Der Name des Projekts (für das Image-Tag)
IMAGE_NAME=who-is-it

# Der Name des lokalen Docker-Compose-Projekts (wird oft aus dem Ordnernamen generiert)
# Finde ihn mit `docker images` und schau nach dem Prefix (z.B. wlkns-ai_who-is-it)
# WICHTIG: Das ist der Name des Ordners, in dem dein Projekt liegt, aber in Kleinbuchstaben.
COMPOSE_PROJECT_NAME=wlkns-ai_who-is-it

# --- Server Konfiguration (optional, für Remote-Deployment) ---

# Benutzer und Hostname deines Servers
REMOTE_USER=server-user
REMOTE_HOST=server.wlkns.org

# Pfad auf dem Server, wo die App deployed werden soll
REMOTE_APP_PATH=/home/server-user/apps/who-is-it
